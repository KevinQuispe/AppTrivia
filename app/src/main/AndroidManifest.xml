<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pcquispe.apptrivia">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_trivia"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_trivia"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".splashscreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PlayTriviaActivity"/>
        <activity
            android:name=".ResultPlayTriviaActivity"/>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".EmpezarTriviaActivity"/>
        <activity android:name=".ReaderActivity" />
        <activity android:name=".RankingTriviaActivity" android:parentActivityName=".ResultPlayTriviaActivity">
        </activity>
    </application>

</manifest>